//登陆拦截
// const redis = require('../config/redis');
// module.exports = {
  // checkLogin: async (ctx, next) => {
    // const sessionId = await redis.get('sessionId');
    // console.log('sessionId', ctx.session.sessionId);
    // console.log('redis', sessionId);
    // if (ctx.session.sessionId !== sessionId) {
    //   ctx.body = "请登录";
    // }
    // next();
  // }
// }
